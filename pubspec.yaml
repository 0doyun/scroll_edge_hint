name: scroll_edge_hint
description: Edge fade hints for Flutter scroll views to make scrollability obvious without persistent scrollbars.
version: 0.1.0
homepage: https://github.com/gong/scroll_edge_hint
repository: https://github.com/gong/scroll_edge_hint
issue_tracker: https://github.com/gong/scroll_edge_hint/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
